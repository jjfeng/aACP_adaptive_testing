import os

from os.path import join
from nestly.scons import SConsWrap
from nestly import Nest
from SCons.Script import Environment, Command, AddOption

Import('env')
localenv = env.Clone()

# Set up state
nest = SConsWrap(Nest(), localenv['output'], alias_environment=localenv)

RUN_STATUS = localenv['clusters']
NUM_SEEDS = localenv['num_seeds']
REUSE_ITERS = 10
SPARSE_P = 1
SPARSE_COEF_VAL = 1.5
FWER = 0.1

nest.add(
    'reuse_test_n',
    [100],
    label_func=lambda c: "test_%d" % c)

nest.add_aggregate("summary_res", list)
nest.add(
    'dp_mech',
    [
        #"no_dp",
        "binary_thres_dp",
        #"bonferroni",
        #"bonferroni_nonadapt",
        "graphical_bonf",
        "graphical_ffs",
        #"graphical_parallel",
    ])

@nest.add_target_with_env(localenv)
def create_dp(env, outdir, c):
    targets = [join(outdir, 'dp_mech.pkl')]
    scratch_file = join("simulation_adversary", outdir, "scratch.txt")
    cmd = [
        'python create_dp_mechanism.py',
        '--parallel-ratio 0.95' if "graphical_parallel" == c['dp_mech'] else '',
        # TODO: this ratio might not hold right now, actually. this was mostly to check.
        '--loss-to-diff-ratio 100000' if "graphical_parallel" == c['dp_mech'] else '',
        '--success-weight 0.5' if "graphical" in c['dp_mech'] else '',
        '--alpha',
        FWER,
        '--dp-mech',
        c['dp_mech'],
	'--scratch',
        scratch_file,
	'--out ${TARGETS[0]}'
    ]
    return env.Command(
        targets,
        [],
        ' '.join(map(str, cmd)))

nest.add(
    'seed',
    range(NUM_SEEDS),
    label_func=lambda c: "seed_%d" % c)

@nest.add_target_with_env(localenv)
def generate_data(env, outdir, c):
    cmd = [
        'python generate_data_single_pop.py',
        '--meta-seed',
        c['seed'],
        '--sparse',
        SPARSE_P,
        '--p',
        REUSE_ITERS,
        '--init-train-n 10',
        '--train-batch-n 0',
        '--train-iters',
        REUSE_ITERS,
        '--init-reuse-test-n',
        c['reuse_test_n'],
        '--init-sparse-beta',
        SPARSE_COEF_VAL,
        '--test-n 4000',
	'--out ${TARGETS[0]}',
	'--log-file ${TARGETS[1]}',
    ]
    return env.Command(
        [
            join(outdir, 'data.pkl'),
            join(outdir, 'log.txt')],
        [],
        ' '.join(map(str, cmd)))

nest.add(
    'model_sim',
    [
        "adversary",
    ])

@nest.add_target_with_env(localenv)
def create_modeler(env, outdir, c):
    cmd = [
        'python create_modeler.py',
        '--simulation',
        c['model_sim'],
        '--min-var-idx',
        SPARSE_P,
        '--preset-coef',
        SPARSE_COEF_VAL,
	'--data ${SOURCES[0]}',
	'--out ${TARGETS[0]}'
    ]
    return env.Command(
        [
            join(outdir, 'model.pkl')],
        c["generate_data"],
        ' '.join(map(str, cmd)))

@nest.add_target_with_env(localenv)
def run_experiment(env, outdir, c):
    cmd = [
        'python main.py',
        '--seed',
	c['seed'],
        '--max-iter',
        REUSE_ITERS,
	'--data ${SOURCES[0]}',
	'--model ${SOURCES[1]}',
	'--dp-mech ${SOURCES[2]}',
	'--log ${TARGETS[0]}',
	'--out-csv ${TARGETS[1]}',
    ]
    targets = [
            join(outdir, 'log.txt'),
            join(outdir, 'res.csv'),
        ]
    c['summary_res'].append(targets[1])
    return env.Command(
        targets,
        [
            c["generate_data"][0],
            c["create_modeler"][0],
            c["create_dp"][0],
        ],
        ' '.join(map(str, cmd)))

nest.pop("dp_mech")

@nest.add_target_with_env(localenv)
def summarize(env, outdir, c):
    cmd = [
        'python summarize.py',
	'--id-cols',
	'dataset,measure,dp,max_iter',
	'--pivot-rows',
	'dataset,measure,dp',
	'--pivot-cols',
	'max_iter',
	'--id-filter',
	REUSE_ITERS,
	'--results',
        ",".join(["simulation_adversary/%s" % r for r in c['summary_res']]),
	'--out-csv ${TARGETS[0]}',
    ]
    return env.Command(
        [
            join(outdir, 'res_summary.csv'),
        ],
        c["summary_res"],
        ' '.join(map(str, cmd)))
